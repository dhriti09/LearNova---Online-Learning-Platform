{
  "name": "online-learning-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel deployment wrapper: builds the React client and serves the Express API as a serverless function.",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.2",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
